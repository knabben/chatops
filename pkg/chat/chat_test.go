package chat

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestNewChat(t *testing.T) {
	assert := assert.New(t)
	//fake_token := "token"

	//chat := NewChat(fake_token)
	assert.Equal(nil, "element")
}
